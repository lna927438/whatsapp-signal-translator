# HelloDog 全面优化对标与验收计划

日期：2026-09-10 UTC。状态：开发评审，未批准上线。

## 发布约束

用户要求：先完成全面优化、验证和预览，只有用户明确确认本次版本上线后才可以部署或更新官网下载。

此前已启动的 0.5.1 发布任务 34533133479 已成功结束。本次修复不包含在该安装包中。本次只保存到独立开发分支，不合并 main、不建立 release/windows-v* 分支、不触发 R2 发布。main 可能触发 Cloudflare 自动部署，因此合并也属于本次发布控制范围。

## 当前最影响使用的问题

截图一是发送进度浮层，截图二是发送结果未确认的恢复面板。源码在准备、翻译、提交和成功阶段均调用浮层方法，因此正常发送也不断打断聊天。未确认表示客户端没有取得足够回执证据，不能据此断言消息没发出或 API 故障。

本轮已修改：

- 正常进度和成功只更新工作区状态栏，不生成聊天浮层，也不抢输入焦点。
- 异常与保留草稿只显示紧凑的“查看”入口，默认收起，用户点击后才出现详情和处理按钮，Escape 收起。
- 操作失败仍保留恢复按钮；处理中同时禁用相互冲突的核对按钮。
- 回执识别兼容 div/span 文本节点、祖先消息 ID、文字方向控制符，以及同一稳定 peer 下的联系人标题变化。
- 保留新消息 ID、相同 peer、匹配译文和导航检查。输入框清空、旧的同文消息、另一个联系人出现相同文字均不能被视为成功。
- 不自动重发状态不明的消息，不修改现有字符计费规则。

限制：这些改动已由模拟 DOM 验证；截图没有提供真实消息 DOM，无法声称所有账号的 WhatsApp 回执兼容问题均已消除。Windows 实测和长时间运行仍是上线前验收项。

## 对标资料与可信范围

这是基于官方公开资料的功能和交互对标，不是安装登录后的性能实测。没有验证竞品延迟、内存、准确率、收费或 Signal 接入能力，不将广告数字作为评测结论。

| 产品 | 已核实资料 | 值得借鉴 | 对 HelloDog 的适用范围 |
| --- | --- | --- | --- |
| HelloGPT | 用户指定 hellogpt.cloud，并提供六张 V1.4.49 客户端截图；具体界面见下方逐图分析 | 分层导航、账号创建、双行工具栏、右键菜单及全局设置分组 | 截图证明控件存在，不证明全部平台可用、性能或菜单执行效果；官方归属未独立核实 |
| SaleSmartly | 官方实时翻译页展示会话翻译和发送时翻译；主页介绍统一消息、客户标签和团队分配 | 在会话内完成翻译、多账号信息整理、客户标签 | 是客服/SCRM 平台，不等同于内嵌 WhatsApp Web；借鉴工作流，不假定接入方式相同 |
| 沉浸式翻译 | 官方输入框文档支持快捷键、语言命令和部分文本翻译 | 输入框原位操作、用户主动触发、快捷键可配置 | 借鉴交互与控制感，不把浏览器扩展等同于多账号桌面工作台 |
| DeepL | 官方术语表页介绍指定术语、上下文适配、多个术语表和共享 | 术语一致性、品牌名和专有名词保护 | 先完善 HelloDog 的翻译质量控制，不代表必须更换当前 DeepSeek 服务 |

来源：

- SaleSmartly 实时翻译：https://www.salesmartly.com/features/real-time-translations/
- SaleSmartly 工作台与客户管理：https://www.salesmartly.com/
- 沉浸式翻译输入框：https://immersivetranslate.com/zh-Hans/docs/input/
- DeepL 术语表：https://www.deepl.com/en/features/glossary

## 当前能力、差距和实施顺序

以下是对 HelloDog 的代码检查与产品建议；“目标”均为拟定验收要求，不是当前实测成绩。

| 优先级 | 方向 | 当前能力/差距 | 下一步设计与验收 |
| --- | --- | --- | --- |
| P0 | 安静发送 | 本轮已去除正常浮层；状态不明仍需要核对 | 正常连续发送不自动展开面板；异常可发现、可收起、可恢复，不能靠隐藏错误冒充成功 |
| P0 | 回执与弱网 | 有任务记录、防重、15 秒前台确认；未知结果仍会阻塞原任务 | 增加按稳定消息 ID 的后台回执复核，分清已提交、平台排队、未确认；恢复时不再扣费或重复发送 |
| P0 | 实测诊断 | 有线路和翻译状态，真实平台 DOM 兼容仍待验证 | 脱敏记录状态码、任务 ID、阶段耗时、匹配失败原因；默认不记录正文、手机号、密钥。分别呈现服务线路、聊天连接、代理检测结果 |
| P1 | 输入体验 | 有 Enter 发送及草稿保护，未提供完整的可选预览模式 | 默认直接翻译发送；可选快捷键翻译预览、编辑后发送、恢复原文；预览不能每条强制弹窗，中文输入法候选确认不触发误发送 |
| P1 | 账号工作台 | 有账号搜索、侧栏收缩、官方样式图标和原生菜单 | 增加置顶、分组、未读与断线标记、最近账号；拖动与快捷切换；长名称和小窗口不挤压聊天区域 |
| P1 | 快捷回复 | 没有完整的快捷回复管理模块 | 新增个人短语、搜索、分类、插入草稿；插入不自动发送；变量未填写时提示。后续才考虑团队共享 |
| P1 | 翻译质量 | 已带最近对话上下文、缓存及联系人语言设置 | 增加术语表、保留词和可选语气；默认保持原意；建立人工评测集覆盖俚语、数字、网址、emoji、混合语言、换行和敏感歧义 |
| P1 | 设置可理解性 | 有新建窗口设置、账号级开关；部分配置与实时状态需进一步区分 | 常用项直接显示，高级项折叠；标明账号默认与联系人覆盖关系；成功保存后才给保存反馈，失败可恢复，改设置后立即验证实际效果 |
| P1 | 多账号运行 | WhatsApp 使用独立会话，打开的视图保留；Signal 是独立进程 | 先测空闲/活跃多账号资源消耗，再设计空闲休眠和唤醒；有待发送任务的账号不自动休眠。测切换、恢复、后台接收和长时间运行 |
| P1 | 独立代理 | WhatsApp 支持 HTTP/HTTPS、免认证 SOCKS5；Signal 未支持 | 代理状态明确区分已配置、检测中、成功、失败；检测时间可见；连接失败不静默直连。Signal 单独验证进程网络隔离后再开放 |
| P1 | 更新与恢复 | 有安装包版本、SHA256、R2 发布校验，主要是完整包 | 增加版本页、更新日志、续传/增量更新可行性、版本回退；升级前备份设置，迁移失败能恢复。弱网不丢已下载内容 |
| P2 | 更多能力 | 尚无完整 OCR、语音翻译、团队角色和分析平台 | 文本主流程稳定后再选：图片/OCR、语音转写、联系人备注、团队协作。每项明确成本、权限与数据保存范围 |

## 页面和模块拆分建议

1. 工作区：账号导航、聊天区域和一行状态摘要；翻译服务名称与线路延迟不占据过多面积。
2. 账号设置：基本信息、语言与翻译、显示、网络四组，代理默认折叠。
3. 输入辅助：翻译预览、快捷回复、恢复原文由输入区主动唤出；不遮挡历史消息。
4. 任务处理：正常进度在状态栏，待核对任务集中在可主动打开的任务列表；不同联系人隔离。
5. 诊断页：将网络问题、平台回执问题、模型问题、余额和登录问题分别说明，提供针对性下一步。
6. 工程边界：逐步将当前 App.vue 分拆为导航、工具栏、状态区和设置入口；平台适配器与任务状态机保持独立，避免视觉修改改变发送行为。

## 上线前验证门槛

- 文本回归：短句、长句、换行、emoji、混合语言、引用消息、相同文本连续发送、账号/联系人切换。
- 输入回归：中文输入法、Enter、Shift+Enter、重复按键、发送中编辑、撤销与粘贴。
- 故障回归：断网、延迟、401/403/429、余额不足、代理错误、进程退出、发送后回执延迟和重启恢复。
- 计费回归：一个任务最多一次上游推理/结算；失败和重放不多扣；预览与确认发送复用同一译文时不重复付费。
- 视觉验收：1366×768 和 1920×1080、100%/125%/150% 缩放、收缩状态、键盘焦点、弱动画设置；各平台在 Windows 实机验证。
- 性能基线：记录冷启动、账号切换、翻译各阶段 p50/p95、空闲 CPU/内存和多账号长时间运行数据；先测再设合理目标，不承诺竞品宣传速度。
- 用户验收：提供未上线的预览、测试结果和待解决项，用户明确确认对应版本后才进入发布流程。

## 本轮交付与后续边界

已实现：安静发送、折叠恢复入口和上述回执兼容补充。
已完成：公开资料对标、差距梳理和分阶段验收设计。
待实现：上表除本轮修复以外的新增功能；不把规划写成已完成。
待用户实测：真实 WhatsApp 消息回执、Signal 操作、代理和 Windows 视觉/长时间运行。


## 2026-09-11：HelloGPT 六张客户端截图补充

证据来自用户本轮上传的六张截图，界面版本标为 V1.4.49。已直接读取图片。没有执行竞品安装包、登录竞品账号或验证其后端。以下可见功能不等于完成运行测试。

| 截图 | 可直接确认的界面 | HelloDog 对应设计 | 尚不能确认 |
| --- | --- | --- | --- |
| 1 应用中心 | 多平台入口卡片；包括 WhatsAppPure、WhatsApp、Signal、TelegramZ/K、LINE、Messenger 等；自定义应用入口；左侧平台图标与角标 | 应用中心、账号导航、聊天区分层；先呈现经过真实适配的 WhatsApp/Signal，后续平台单独验收 | 各入口登录、翻译、接收和会话隔离是否完整；WhatsAppPure 的具体行为 |
| 2 新增窗口 | 名称、翻译通道、线路及延迟显示、收发翻译、双向语言、计数器、独立代理开关、群翻、禁止中文、字号、颜色、批量创建数量 | 现有创建面板已有多数基础项；新增可复用配置模板、批量数量及命名预览；默认批量只创建账号记录，不同时加载全部网页 | 计数器到底统计什么；代理协议和验证行为；69 ms 测量对象；批量失败回滚方式 |
| 3 主工作区 | 全局导航、平台入口、三个账号页签、双行翻译控制；原生 WhatsApp 会话区 | 顶部保留必要状态，其他控制折叠；图标加完整名称提示；账号级配置和联系人覆盖关系清楚显示 | 平台切换内存成本、会话恢复速度；原生通话提示不证明具备语音翻译 |
| 4 收缩布局 | 全局导航收起后聊天区扩大；红框标记 WhatsApp 会话列表和内容区边界 | 优先让 HelloDog 自有侧栏支持宽度调节与收缩；若调 WhatsApp 内部列表，独立适配并保留恢复原布局入口 | 红框不是可拖拽功能的证据；静态图不能确定宽度调整手势 |
| 5 右键菜单 | 前进后退、刷新、删除、编辑、关闭会话、复制地址、删除全部、回主页、IP、日志、译文显隐、缩放 | 常用动作置前；网络和诊断放更多操作；明确关闭窗口与移除账号区别；批量删除显示范围并二次确认；聊天原生右键不被覆盖 | 删除是否清理登录状态、关闭是否终止进程、IP 显示是哪条网络出口 |
| 6 全局设置 | 历史消息手动/自动翻译、关闭发送翻译后的行为、分号替换、尾句号处理、语音翻译、回翻、双输入框、横竖页签、闪烁/系统/声音提醒、会话开启方式、硬件加速、启动/最小化/托盘相关选择、语言、清空数据 | 设置拆为翻译、通知、外观、运行、数据；默认低打扰；预览与回翻按需；历史翻译明确范围和费用；数据清理按类型选择 | 语音支持哪些平台、音频时长限制、回翻费用、系统通知权限、各项实际保存和即时生效情况 |

### 对照截图后的新增优先事项

1. **历史消息翻译策略（P0）**：当前 WhatsApp 适配器会扫描可见消息。打开旧对话时应能选择仅新消息、选中/当前可见历史、手动翻译；明确初次加载边界与虚拟列表重复挂载，防止滚动反复调用或意外花费。默认不能悄悄批量翻译全部历史。
2. **通知与异常分级（P0）**：正常发送零弹窗；失败任务可主动查看；应用前台不重复发系统通知；后台通知和声音分别设置；锁屏时默认隐藏正文。先保证不漏掉真正需要处理的任务。
3. **账号模板与批量创建（P1）**：配置复用、名称编号预览、数量上限、部分失败结果清楚；每个账号仍独立会话；代理凭据不在批量预览中明文展示；创建后仅激活选中账号，避免启动卡顿。
4. **可选预览、双输入框与回翻（P1）**：默认保持直接翻译发送；用户选择后才展开原文/译文；回翻默认关闭，启用前说明额外调用成本；恢复原文不误触发送；不能用回翻结果保证翻译完全正确。
5. **布局与高效导航（P1）**：全局导航和账号列可分别收缩；账号列可调宽、长名称可读；保留未读提示、搜索与键盘切换；横竖页签作为后续可选项，不使基础界面复杂化。
6. **运行设置（P1）**：Windows 自启、关闭到托盘、启动最小化、后台通知与恢复流程；硬件加速放高级设置并注明需重启；保护未完成发送任务，不能用“一键重启”掩盖稳定性问题。
7. **翻译风格（P1/P2）**：术语、姓名、数字和网址保护优先；标点处理仅作明确的可选规则，不默认全局替换；语音/OCR 在文本链路稳定后独立测成本、准确性和平台兼容。

### 交互验收用例补充

- 连续发送 100 条正常文本时，不出现自动展开的发送进度或成功面板；故障时有明确入口可处理。此为验收目标，尚未完成真人平台测试。
- 打开历史对话但未授权历史翻译，不因滚动或切换而新增历史翻译扣费。
- 批量创建后只加载当前选中窗口；失败账号可单独重试，已有账号不重复生成。
- 关闭窗口、移除账号、清除缓存、清除登录状态的文案和后果一致，任务状态不被静默清空。
- 侧栏收起、调宽及系统缩放后，聊天区域、点击位置和键盘焦点仍一致。
- 通知关闭后不发系统提示；前台聊天不重复打扰；后台提醒跳转到正确账号。

本次新增的是截图证据、差距和验收设计，没有声称以上功能均已实现。继续沿用开发分支和用户批准后发布的规则。
