Place a current signal-cli distribution here before packaging, for example:
resources/signal/bin/signal-cli.bat   (Windows)
resources/signal/bin/signal-cli       (macOS/Linux)

Alternatively set SIGNAL_CLI_PATH or put signal-cli on PATH during development.
